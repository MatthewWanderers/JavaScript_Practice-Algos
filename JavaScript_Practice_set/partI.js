// function madLib(verb, adjective, noun) {
//   console.log(`We shall ${verb} the ${adjective} ${noun}.`);
// }

const madLib = (verb, adjective, noun) => {
  console.log(`We shall ${verb} the ${adjective} ${noun}.`);
}
madLib("make", "best", "cake");

const isSubstring = (string, sub) => {
  return string.includes(su
}

isSubstring
